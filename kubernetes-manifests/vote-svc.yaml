apiVersion: v1
kind: Service
metadata:
  name: voting-service
  labels:
    name: vote-svc
    app: voting-app
spec:
  type: LoadBalancer
  selector:
    name: voting-app-pod
    app: voting-app
  ports:
  - port: 80
    targetPort: 80
